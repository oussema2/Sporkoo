export type CrudParamsType = {
  id: string
}
export type Pagination = {
  page: number
  limit: number
  totalPages: number
  totalItems: number
}

export type PaginationParams = {
  page: number
  limit: number
}
